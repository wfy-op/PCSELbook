\begin{figure}[htbp]
  \centering
  \resizebox{0.98\textwidth}{!}{%
  \begin{tikzpicture}[
    node distance=6mm and 8mm,
    src/.style={rectangle, rounded corners, draw, align=center, fill=blue!12, minimum width=2.8cm, minimum height=8mm},
    mid/.style={rectangle, rounded corners, draw, align=center, fill=orange!12, minimum width=2.9cm, minimum height=8mm},
    dst/.style={rectangle, rounded corners, draw, align=center, fill=green!12, minimum width=2.6cm, minimum height=8mm},
    arr/.style={-Stealth, thick}
  ]
    \node[src] (s1) {孔半径偏差 / 晶格位移};
    \node[src, below=of s1] (s2) {刻蚀深度偏差 / 侧壁角变化};
    \node[src, below=of s2] (s3) {金属与孔阵错位};
    \node[src, below=of s3] (s4) {边缘截断 / 粗糙度};

    \node[mid, right=18mm of s1] (m1) {相位网络失谐};
    \node[mid, right=18mm of s2] (m2) {辐射通道再分配};
    \node[mid, right=18mm of s3] (m3) {注入不均匀};
    \node[mid, right=18mm of s4] (m4) {热热点迁移};

    \node[dst, right=19mm of m1] (d1) {$\Delta G_{\star j}$};
    \node[dst, below=of d1] (d2) {品质因子与远场};
    \node[dst, below=of d2] (d3) {偏振纯度};
    \node[dst, below=of d3] (d4) {$T_{\max}$ 与良率};

    \draw[arr] (s1) -- (m1);
    \draw[arr] (s1) -- (m2);
    \draw[arr] (s2) -- (m2);
    \draw[arr] (s3) -- (m3);
    \draw[arr] (s3) -- (m4);
    \draw[arr] (s4) -- (m1);
    \draw[arr] (s4) -- (m4);

    \draw[arr] (m1) -- (d1);
    \draw[arr] (m1) -- (d2);
    \draw[arr] (m2) -- (d2);
    \draw[arr] (m2) -- (d3);
    \draw[arr] (m3) -- (d1);
    \draw[arr] (m3) -- (d4);
    \draw[arr] (m4) -- (d4);
  \end{tikzpicture}
  }
  \caption{容差分析中的误差传播图：工艺偏差先映射为物理机制变化，再传递到可观测器件指标。}
  \label{fig:ch20b_error_propagation_graph}
\end{figure}
